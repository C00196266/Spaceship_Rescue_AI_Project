#include "World.h"

World::World() {
	//for (int i = 0; i < 20; i++) {
	//	m_walls.push_back(new Wall(sf::Vector2f(i * 32, 0)));
	//}

	//for (int i = 0; i < 20; i++) {
	//	if (i % 2 == 0) {
	//		m_floors.push_back(new Floor(sf::Vector2f(i * 32, 0), 0));
	//	}
	//	else {
	//		m_floors.push_back(new Floor(sf::Vector2f(i * 32, 0), 1));
	//	}
	//}
}

void World::render(sf::RenderWindow &window) {
	//for (int i = 0; i < m_walls.size(); i++) {
	//	m_walls.at(i)->render(window);
	//}

	//for (int i = 0; i < m_floors.size(); i++) {
	//	m_floors.at(i)->render(window);
	//}
}

void World::update(float deltaTime) {

}